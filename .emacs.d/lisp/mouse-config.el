(global-set-key [mouse-4]
                '(lambda ()
                   (interactive)
                   (scroll-down 1)))

(global-set-key [wheel-down]
                '(lambda ()
                   (interactive)
                   (scroll-up 1)))

(global-set-key [mouse-5]
                '(lambda ()
                   (interactive)
                   (scroll-up 1)))

(global-set-key [wheel-up]
                '(lambda ()
                   (interactive)
                   (scroll-down 1)))
